apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: digital-bank
  labels:
    app: vault
spec:
  type: ClusterIP
  ports:
    - port: 8200
      targetPort: 8200
      protocol: TCP
  selector:
    app: vault